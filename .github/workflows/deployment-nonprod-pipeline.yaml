name: Deploy to Non-Prod

on:
  push:
    branches: 
      - nonprod
  pull_request:
    branches:
      - nonprod
  workflow_dispatch:  # ADDED: Allow manual trigger for testing

permissions:
  id-token: write      # Required for OIDC
  contents: read
  pull-requests: write
  security-events: write

jobs:
  security-scan-iac:
    name: Checkov Security Scan
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1

      - name: Mark workspace as safe for Git
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Run Checkov IaC Security Scan
        uses: bridgecrewio/checkov-action@v12.2867.0
        env:
            GIT_CONFIG_GLOBAL: /tmp/.gitconfig
        with:
          directory: .
          framework: terraform
          output_format: cli,sarif
          output_file_path: console,checkov-results.sarif
          soft_fail: false
          download_external_modules: true
          skip_check: CKV_AWS_51,CKV_AWS_136,CKV_AWS_260,CKV2_AWS_5
          

      - name: Upload Checkov Results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3.26.13
        if: always()
        with:
          sarif_file: checkov-results.sarif

  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-22.04
    needs: security-scan-iac
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.7

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4.0.2
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN_NONPROD }}
          aws-region: ${{ vars.AWS_REGION }}
          role-session-name: GHA-NonProd-Plan-${{ github.sha }}
          role-duration-seconds: 3600

      - name: Initialize Terraform
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET_NONPROD }}" \
            -backend-config="key=nonprod/terraform.tfstate" \
            -backend-config="region=${{ vars.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ vars.TF_STATE_LOCK_TABLE }}"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Plan
        run: terraform plan -out=tfplan.binary

      - name: Convert Plan to JSON for Security Analysis
        run: terraform show -json tfplan.binary > tfplan.json

      - name: Run Checkov on Terraform Plan
        uses: bridgecrewio/checkov-action@v12.2867.0
        with:
          file: tfplan.json
          framework: terraform_plan
          output_format: cli,sarif
          output_file_path: console,checkov-plan-results.sarif
          soft_fail: false
          skip_check: CKV_AWS_51,CKV2_AWS_5,CKV_AWS_260

      - name: Upload Checkov Plan Results
        uses: github/codeql-action/upload-sarif@v3.26.13
        if: always()
        with:
          sarif_file: checkov-plan-results.sarif

      - name: Upload Terraform Plan
        uses: actions/upload-artifact@v4.4.3
        with:
          name: tfplan-nonprod
          path: tfplan.binary
          retention-days: 5

  terraform-apply:
    name: Apply Infrastructure
    needs: terraform-plan
    runs-on: ubuntu-22.04
    if: github.event_name == 'push'  # Only auto-deploy on push, not PRs
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.7

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4.0.2
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN_NONPROD }}
          aws-region: ${{ vars.AWS_REGION }}
          role-session-name: GHA-NonProd-Apply-${{ github.sha }}
          role-duration-seconds: 3600

      - name: Initialize Terraform
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET_NONPROD }}" \
            -backend-config="key=nonprod/terraform.tfstate" \
            -backend-config="region=${{ vars.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ vars.TF_STATE_LOCK_TABLE }}"

      - name: Download Terraform Plan
        uses: actions/download-artifact@v4.1.8
        with:
          name: tfplan-nonprod

      - name: Apply Infrastructure
        run: terraform apply -auto-approve tfplan.binary

  build-and-scan-image:
    name: Build & Scan Container Image
    needs: terraform-apply
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4.0.2
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN_NONPROD }}
          aws-region: ${{ vars.AWS_REGION }}
          role-session-name: GHA-NonProd-Build-${{ github.sha }}
          role-duration-seconds: 3600

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2.0.1

      - name: Create ECR Repository if not exists
        env:
          ECR_REPOSITORY: demo-app-images
          AWS_REGION: ${{ vars.AWS_REGION }}
        run: |
          aws ecr describe-repositories --repository-names ${ECR_REPOSITORY} --region ${AWS_REGION} || \
          aws ecr create-repository \
            --repository-name ${ECR_REPOSITORY} \
            --image-scanning-configuration scanOnPush=true \
            --region ${AWS_REGION}

      - name: Build Container Image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: demo-app-images
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -f docker/Dockerfile docker/
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:nonprod-latest

      - name: Run Trivy Security Scan
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ steps.login-ecr.outputs.registry }}/demo-app-images:${{ github.sha }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'  # Don't fail build in nonprod
          ignore-unfixed: true  # Only report vulnerabilities with available fixes

      - name: Upload Trivy Results
        uses: github/codeql-action/upload-sarif@v3.26.13
        if: always()
        with:
          sarif_file: trivy-results.sarif

      - name: Push Image to ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: demo-app-images
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:nonprod-latest

  notify-discord:
    name: Discord Notification
    runs-on: ubuntu-22.04
    if: always()
    needs: [security-scan-iac, terraform-plan, terraform-apply, build-and-scan-image]
    steps:
      - name: Send Status to Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SEC_STATUS: ${{ needs.security-scan-iac.result == 'success' && '✅ Passed' || '❌ Failed' }}
          PLAN_STATUS: ${{ needs.terraform-plan.result == 'success' && '✅ Passed' || '❌ Failed' }}
          APPLY_STATUS: ${{ needs.terraform-apply.result == 'success' && '✅ Applied' || '❌ Failed' }}
          BUILD_STATUS: ${{ needs.build-and-scan-image.result == 'success' && '✅ Passed' || '❌ Failed' }}
          OVERALL_STATUS: ${{ (needs.security-scan-iac.result == 'failure' || needs.terraform-plan.result == 'failure' || needs.terraform-apply.result == 'failure' || needs.build-and-scan-image.result == 'failure') && '❌ FAILED' || '✅ SUCCESS' }}
          OVERALL_COLOR: ${{ (needs.security-scan-iac.result == 'failure' || needs.terraform-plan.result == 'failure' || needs.terraform-apply.result == 'failure' || needs.build-and-scan-image.result == 'failure') && '15158332' || '3066993' }}
        run: |
          cat > payload.json <<JSON
          {
            "embeds": [{
              "title": "🔧 NonProd Deployment",
              "description": "**Status:** ${OVERALL_STATUS}\n**Branch:** `${{ github.ref_name }}`\n**By:** ${{ github.actor }}",
              "color": ${OVERALL_COLOR},
              "fields": [
                {"name": "🔒 Security", "value": "${SEC_STATUS}", "inline": true},
                {"name": "📋 Plan", "value": "${PLAN_STATUS}", "inline": true},
                {"name": "🚀 Apply", "value": "${APPLY_STATUS}", "inline": true},
                {"name": "🐳 Build", "value": "${BUILD_STATUS}", "inline": true},
                {"name": "🔗 Details", "value": "[View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})", "inline": false}
              ],
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)"
            }]
          }
          JSON

          curl -sS -X POST "$DISCORD_WEBHOOK" -H "Content-Type: application/json" --data @payload.json